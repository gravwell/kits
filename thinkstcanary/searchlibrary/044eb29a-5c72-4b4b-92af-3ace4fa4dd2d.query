tag=$CANARY_AUDIT json action_type flock_id flock_name message timestamp user user_ip
| eval in(action_type, 
    "flock_alter_config"
    )
| eval message ~"SMS"
| regex -e message "(?P<operation>\w+)"
| lower operation
| regex -e message "numbers\:\s(?P<sms_list>.*?)?\;"
| regex -e message "(removed)\:\s(?P<sms_removed>.*)"
| regex -e message "(?P<event_type>notification SMS numbers.*?)?\:"
| unique timestamp
| printf -e context "at %v, %v (%v) %v %v by removing %v from SMS List: \"%v\"" timestamp user user_ip operation event_type sms_removed sms_list
| $CANARY_ALERT_META("Canary", "Flock Management", "Low", "Number(s) Removed from SMS Notification List")