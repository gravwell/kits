tag=$CANARY_AUDIT json action_type flock_id flock_name message timestamp user user_ip
| eval in(action_type, 
    "flock_alter_config"
    )
| eval message ~"SMS"
| regex -e message "(?P<operation>\w+)"
| lower operation
| regex -e message "numbers\:\s(?P<sms_list>.*?)?\;"
| regex -e message "(added)\:\s(?P<sms_added>.*)"
| regex -e message "(?P<event_type>notification SMS numbers.*?)?\:"
| unique timestamp
| printf -e context "at %v, %v (%v) %v %v by adding %v to SMS List: \"%v\"" timestamp user user_ip operation event_type sms_added sms_list
| $CANARY_ALERT_META("Canary", "Flock Management", "Low", "Number(s) Added to SMS Notification List")