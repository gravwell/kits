tag=zeekconn ax resp_port resp_bytes
| stats count by resp_port
/* Only looking at ports in the lower range */
| eval resp_bytes > 140 && resp_port < 16000
| sort by count asc
| table count resp_port
