tag=$GITHUB_AUDIT json @timestamp actor action operation_type repo
| eval in(action, 
    "repo.transfer"
    )
| printf -e context "at %v, %v was %v by %v for %v" @timestamp action operation_type actor repo
| $GITHUB_ALERT_META("Medium", "User Accepted Repo Transfer Request")