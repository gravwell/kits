tag=$GITHUB json @timestamp actor action operation_type org repo
| eval in(action, 
    "org.runner_group_created"
    )
| printf -e context "at %v, %v was %v by %v in %v" @timestamp action operation_type actor org
| $GITHUB_ALERT_META("Low", "Runner Group Created")