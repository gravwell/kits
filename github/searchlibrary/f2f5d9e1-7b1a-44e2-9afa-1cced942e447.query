tag=$GITHUB json @timestamp actor action operation_type business repo
| eval in(action, 
    "private_repository_forking.clear"
    )
| printf -e context "at %v, %v was %v by %v for %v in %v" @timestamp action operation_type actor repo business
| $GITHUB_ALERT_META("Low", "Private Forking Cleared")