tag=$GITHUB_AUDIT json @timestamp action actor business operation_type repo
| eval in(action, 
    "business_secret_scanning.disable"
    )
| printf -e context "at %s, %s was %s by %s in %s" @timestamp action operation_type actor business
| $GITHUB_ALERT_META("High", "Secret Scanning Disabled for Enterprise")