tag=$GITHUB_AUDIT json @timestamp actor action operation_type business repo
| eval in(action, 
    "business_dependabot_alerts.disable"
    )
| printf -e context "at %v, %v was %v by %v for %v" @timestamp action operation_type actor business
| $GITHUB_ALERT_META("High", "Dependabot Alerts Disabled")