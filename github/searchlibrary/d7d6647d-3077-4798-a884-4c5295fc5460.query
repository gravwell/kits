tag=$GITHUB_AUDIT json @timestamp actor action operation_type repo
| eval in(action, 
    "repo.create_actions_secret"
    )
| printf -e context "at %v, %v was %v by %v for %v repo" @timestamp action operation_type actor repo
| $GITHUB_ALERT_META("Low", "New Secret Created")