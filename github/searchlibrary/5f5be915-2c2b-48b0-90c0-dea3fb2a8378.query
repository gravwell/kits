tag=$GITHUB_AUDIT json @timestamp actor action operation_type org repo
| eval in(action,  
    "org.transfer",
    "org.transfer_outgoing"
    )
| printf -e context "at %v, %v was %v by %v for %v" @timestamp action operation_type actor org
| $GITHUB_ALERT_META("Medium", "Org Transfer Between Enterprise Accounts")