tag=$GITHUB_AUDIT json SRC user actor actor_ip repo action~"pull"
| eval if (!has(actor_ip) || actor_ip == "") {actor_ip = "No Data";}
| eval if (!has(user) || user == "") {user = "No Data";}
| eval actor_ip~"%%2%%"
| stats unique_count by action
| table actor_ip actor action unique_count