tag=$OKTA_USERS
| json
    profile.firstName
    profile.lastName
    profile.manager
| eval managerDisplay = manager;
    if (!has(manager) || manager == "" || manager == "null" || match(manager, "(?i)^true$")) {
        managerDisplay = firstName + " " + lastName;
    }
| stats unique_count as count by managerDisplay
| numbercard count