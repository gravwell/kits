tag=$OKTA_SYSTEM json 
    eventType 
    outcome.result
    actor.alternateId as actorAltId
    target.alternateId as targetAltId
| lookup -r okta_eventTypes eventType eventType (category subcategory)
| grep -e category "%%okta_category%%"
| stats count by category subcategory eventType actorAltId targetAltId result 
| table category subcategory eventType actorAltId targetAltId result count