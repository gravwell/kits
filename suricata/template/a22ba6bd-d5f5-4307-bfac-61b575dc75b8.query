tag=$SURICATA json event_type=="http" src_ip dest_ip http.hostname as hostname http.url as url http.http_method as method http.status as status
| eval (src_ip == "%%1%%" || dest_ip == "%%1%%")
| table TIMESTAMP src_ip method hostname url status