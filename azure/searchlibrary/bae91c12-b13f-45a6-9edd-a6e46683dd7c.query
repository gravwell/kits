tag=$AZURE_AD json operationName identity as actor resourceId correlationId
| eval (
    operationName == "Remove owner from application" ||
    operationName == "Remove service principal"
  )
| table