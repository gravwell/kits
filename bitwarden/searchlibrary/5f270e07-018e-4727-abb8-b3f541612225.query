@nonsuperusers{dump -r bitwardenMembers
| eval !in(userType,"Owner","Admin")
| table email userId};
tag=$BITWARDEN_TAG ax
| lookup -s -r @nonsuperusers actingUserId userId email
| lookup -r bitwardenEventTypes eventType type event
| stats count by event
| chart count by event
