{
	"Name": "LLM_ASSISTANT_REPLIES",
	"Description": "Declares the LLM intrinsic EVs and narrows to model replies. Goes in the extraction position, immediately after the tag block, NOT after a pipe. Append any further enumerated values the query needs: tag=$LLM_TAG $LLM_ASSISTANT_REPLIES model session_id | ...",
	"Labels": [
		"llm",
		"ai"
	]
}