{
	"Name": "LLM_USAGE",
	"Description": "Declares the LLM intrinsic EVs and narrows to token accounting events. Requires Log-Usage=true on the listener. Goes in the extraction position, immediately after the tag block, NOT after a pipe. Append any further enumerated values the query needs: tag=$LLM_TAG $LLM_USAGE model session_id | ...",
	"Labels": [
		"llm",
		"ai"
	]
}