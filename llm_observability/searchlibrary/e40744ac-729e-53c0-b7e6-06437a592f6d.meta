{
	"Name": "Search - LLM - Token Totals by Model [table]",
	"Description": "Prompt, completion, and total tokens per model with a request count, sorted by spend.\n\nRequires Log-Usage=true on the listener.\n\nThe LLM ingester attaches its fields as intrinsic enumerated values, so the query declares the ones it needs on the `intrinsic` extraction module in the tag block.",
	"GUID": "e40744ac-729e-53c0-b7e6-06437a592f6d",
	"Labels": [
		"llm",
		"ai"
	],
	"Metadata": {
		"timeframe": null
	}
}