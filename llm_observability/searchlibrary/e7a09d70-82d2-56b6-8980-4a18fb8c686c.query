tag=$LLM_TAG $LLM_ASSISTANT_REPLIES session_id model listener embeddings
| semantic -t $LLM_SEMANTIC_THRESHOLD -score risk_score "I cannot help with that request"
| sort by risk_score desc
| limit 100
| table TIMESTAMP risk_score session_id model listener DATA
