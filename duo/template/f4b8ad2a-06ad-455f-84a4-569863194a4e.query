tag=$DUO
$DUO_EVX
$DUO_NORMALIZE_EVENTTYPE
| lookup -r duo_eventTypes eventType value (category subcategory description)
| stats count by category
| grep -e category "%%duo_category%%"
| sort by count desc
| table category count