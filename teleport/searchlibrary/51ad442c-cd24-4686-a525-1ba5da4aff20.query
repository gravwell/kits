tag=$TELEPORT_AUDIT ax event=="exec" user login command exitCode
| eval command ~ "curl" || command ~ "wget" || command ~ "nc " || command ~ "ncat" || command ~ "netcat" || command ~ "base64 -d" || command ~ "base64 --decode" || command ~ "chmod +x" || command ~ "chmod 7"
| table user login command exitCode
