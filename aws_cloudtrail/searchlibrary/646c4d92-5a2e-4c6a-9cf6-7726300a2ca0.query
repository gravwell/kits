tag=$AWS_CLOUDTRAIL json eventName eventSource
| eval (eventSource == "elasticloadbalancing.amazonaws.com" && (
    eventName == "ApplySecurityGroupsToLoadBalancer" ||
    eventName == "SetSecurityGroups"
    )
)