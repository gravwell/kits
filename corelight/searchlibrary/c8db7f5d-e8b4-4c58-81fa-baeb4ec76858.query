tag=corelight_x509 ax
| eval (if (TIMESTAMP < time($(certificate.not_valid_before))) {Validity="NOT_YET_VALID";} if (TIMESTAMP > time($(certificate.not_valid_after))) {Validity="EXPIRED";})
| require Validity
| table "certificate.subject" "certificate.issuer" "san.dns" "certificate.not_valid_after" "certificate.not_valid_before" Validity
