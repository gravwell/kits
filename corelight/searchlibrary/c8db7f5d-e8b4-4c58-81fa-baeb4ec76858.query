tag=corelight_x509 ax
| eval (TIMESTAMP < time($(certificate.not_valid_before)) || TIMESTAMP > time($(certificate.not_valid_after)))
| time "certificate.not_valid_after" expired_on
| table "certificate.subject" "certificate.issuer" "san.dns" expired_on
